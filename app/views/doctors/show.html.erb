<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to appt.patient.name, appt.patient %></li>
    <li><%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %></li>
  <% end %>
</ul>
