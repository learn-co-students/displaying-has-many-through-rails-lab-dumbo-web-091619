<ul>
    <% @patient.appointments.each do |appt| %>
      <li><%= link_to appt.doctor.name, appt.doctor %></li>
      <li><%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %></li>
    <% end %>
  </ul>
  